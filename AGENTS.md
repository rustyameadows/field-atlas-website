# Agent Notes

- Never use Google Chrome or Chrome-based tooling for this repo.
- Use the built-in browser only for browser verification.
- If the built-in browser is unavailable, stop and tell the user instead of
  falling back to Chrome.
- Do not stop before completing the agreed plan and task list unless a real
  blocker prevents progress.
- Keep working through implementation, verification, and a clear handoff.
